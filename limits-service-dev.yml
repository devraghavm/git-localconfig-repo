management:
  endpoints:
    web:
      exposure:
        include: '*'
  security:
    enabled: false
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
limits-service:
  minimum: 1