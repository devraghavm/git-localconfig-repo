server:
  port: 8761
management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - bus-refresh
          - health
  security:
    enabled: false
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false